---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "sweb_site Resource - sweb"
subcategory: ""
description: |-
  Manages one shared-hosting website (add/edit/del on /sites). Identified by its document root (doc_root); domain, machine and enable_redis_session are set at creation and force replacement, while alias updates in place.
---

# sweb_site (Resource)

Manages one shared-hosting website (add/edit/del on /sites). Identified by its document root (`doc_root`); `domain`, `machine` and `enable_redis_session` are set at creation and force replacement, while `alias` updates in place.

## Example Usage

```terraform
# Manage a shared-hosting website (document root) on a domain the account owns.
# Identified by doc_root; alias updates in place, the binding forces replacement.
resource "sweb_site" "shop" {
  alias    = "shop"
  doc_root = "shop"
  domain   = "example.com"
  # machine              = "www"  # optional subdomain label (forces replace)
  # enable_redis_session = true   # store PHP sessions in Redis (forces replace)
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `alias` (String) The site name (display alias). Updated in place via edit.
- `doc_root` (String) The site's home directory — its stable identity and the edit/del key.
- `domain` (String) The domain the site is created under (must already belong to the account).

### Optional

- `enable_redis_session` (Boolean) Whether to store PHP sessions in Redis. Set at creation only.
- `machine` (String) Subdomain label to bind the site to (optional).

### Read-Only

- `doc_root_full` (String) The absolute document root reported by the API.
- `id` (String) Resource id (equal to doc_root).
- `site_id` (Number) The API's numeric site id.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import an existing website by its document root (as shown by `sweb hosting site list`).
# The binding inputs (domain/machine/enable_redis_session) aren't per-site
# API-reported, so set them in your HCL afterwards.
terraform import sweb_site.shop shop
```
